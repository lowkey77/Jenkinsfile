Jenkinsfile (Declarative Pipeline)
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World"'
                sh javac HelloWorld.java
                    echo "Multiline shell steps works too"
                    ls -lah
                '''
            }
        }
    }
}
